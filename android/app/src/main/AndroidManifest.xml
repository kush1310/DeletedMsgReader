<?xml version="1.0" encoding="utf-8"?>
<!--
  AndroidManifest.xml — NotiCatch (WhatsApp Notification Saver)
  
  PRD & SRS Security Constraints:
    - INTERNET permission is strictly OMITTED to guarantee air-gapped local privacy.
    - Principle of least privilege: only system notification listener & background services.
    - android:allowBackup="false" to prevent adb backup of encrypted database.
    - FLAG_SECURE prevents screenshot and task switcher leakage.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.noticatch.app">

    <!-- =========================================================
         Permissions (Air-Gapped: NO android.permission.INTERNET)
         ========================================================= -->

    <!-- Core: Binds the NotificationListenerService to the Android framework -->
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" />

    <!-- Persistent background execution -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Android 13+ (API 33+): Required for foreground service status notification -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Biometric authentication via BiometricPrompt -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <!-- Battery optimization exemption for Doze survival -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Wake lock to prevent service drops during screen-off -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- =========================================================
         Application Configuration
         ========================================================= -->
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NotiCatch"
        android:usesCleartextTraffic="false"
        tools:targetApi="34">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Notification Listener Service -->
        <service
            android:name=".service.NotificationListener"
            android:exported="true"
            android:label="@string/notification_listener_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Foreground Daemon Service -->
        <service
            android:name=".service.ForegroundDaemon"
            android:exported="false"
            android:foregroundServiceType="specialUse">
        </service>

    </application>

</manifest>
